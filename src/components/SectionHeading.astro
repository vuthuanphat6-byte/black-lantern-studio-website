---
const {title,label,href}=Astro.props;
---
<div class="section-heading"><h2>{title}</h2><span class="heading-rule" aria-hidden="true"></span>{href&&<a class="text-link" href={href}>{label||'Xem tất cả'} ↗</a>}</div>
