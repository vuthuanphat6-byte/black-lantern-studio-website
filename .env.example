# No credentials in source. Production values are configured by the systemd unit.
PORT=4321
HOST=127.0.0.1
SITE_ORIGIN=http://127.0.0.1:4321
NODE_ENV=development
TRUST_CLOUDFLARE=0
# CONTACT_DIR points outside the public static tree.
